/** Number of rows on the board (targets 1–12). */
export const SLOTS = 12

/** Maximum pearls per row at game start. */
export const MAX_PEARLS = 7

/** Maximum number of players per game. */
export const MAX_PLAYERS = 5

/** Minimum number of players required to start a game. */
export const MIN_PLAYERS = 2

/** Minimum ms between reactions from the same spectator in the same game. */
export const REACTION_THROTTLE_MS = 1_000
